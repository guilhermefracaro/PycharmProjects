import math
num = int(input('Digite um número: '))
rq = math.sqrt(num)
print('A raíz quadrada de {} é {:.2f}'.format(num, rq))

from math import sqrt
num = int(input('Digite um número: '))
rq = sqrt(num)
print('A raíz quadrada de {} é {:.2f}'.format(num, rq))
